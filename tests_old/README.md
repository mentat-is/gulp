# deprecated tests using the old python sdk

these are the tests made with the [old python sdk](https://github.com/mentat-is/gulp-sdk-python), they are provided as reference to reimplement the missing tests using the new [gulp-sdk](https://github.com/mentat-is/gulp-sdk).

> once all the new tests are implemented, these can be removed and the old sdk can be archived or deleted.

