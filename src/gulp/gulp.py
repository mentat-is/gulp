"""
The `gulp` module provides a wrapper around the main entry point.

It is designed to be used as a command-line interface (CLI) for the Gulp framework,
"""

# pylint: disable=unused-import
from gulp.__main__ import main
